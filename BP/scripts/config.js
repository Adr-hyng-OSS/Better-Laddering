export default {
  /**
   * Enables debug messages to content logs.
   */
  debug: true,
  /**
   * Show's where ladder to put in
   */
  showParticle: false,
};

// version (do not change)
export const VERSION = "1.0.0";